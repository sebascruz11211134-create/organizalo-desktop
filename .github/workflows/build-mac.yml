# Workflow desactivado — el proyecto migrĂł a web pura (Vercel).
# El build del instalador Mac ya no aplica.
# Deploy automatico lo maneja Vercel al hacer push a main.

name: Build & Deploy Mac Installer (DESACTIVADO)

on:
  workflow_dispatch:  # Solo manual — no corre automaticamente

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow esta desactivado. El frontend se despliega en Vercel automaticamente."
